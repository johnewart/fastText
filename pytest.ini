[pytest]
testpaths = python/fasttext_module/fasttext/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    unit: Unit tests that don't require external data
    integration: Integration tests that require test data

